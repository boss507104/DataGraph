[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "DataGraph"
version = "2.0.0"
description = "Publication-quality matplotlib styling"
authors = [
    {name = "Hanseul Kang", email = "hanseul.kang@aalto.fi"}
]
dependencies = [
    "numpy",
    "matplotlib",
    "rich",
]